"""
MCP surface package (named mcp_server to avoid shadowing the installed
`mcp` SDK distribution). Owns contract identifiers, tool/resource
registration, services bundle, and MCP-facing metrics.
"""
